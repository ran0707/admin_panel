// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#00c04b',
    },
    secondary: {
      main: '#ff4081',
    },
  },
});

export default theme;
